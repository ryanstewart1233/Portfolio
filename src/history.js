import { createBrowserHistory } from 'history'; //this is a dependancy of react router 

export default createBrowserHistory();

//we create and export this file so that we get control over the browser history so it makes it easier to programatically navigate around users